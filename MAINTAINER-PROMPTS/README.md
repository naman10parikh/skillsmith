# MAINTAINER-PROMPTS

Raw maintainer directives that shape skillsmith's direction land here (one file per prompt). Distilled tasks flow into `memory/` and the doc spine. Empty until the first directive is filed.
