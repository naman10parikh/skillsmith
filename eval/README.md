# eval

The eval harness for the skillsmith CLI — golden tasks that gate quality (build → run → score) before a release. To be layered in as the CLI's behavior stabilizes. Empty until the first eval is written.
