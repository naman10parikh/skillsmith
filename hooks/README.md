# hooks

skillsmith-specific lifecycle hooks (distinct from the 14 inherited Claude Code hooks in `.claude/hooks/`). Use this for hooks that fire on skillsmith's own events. Empty until the first project-specific hook exists.
