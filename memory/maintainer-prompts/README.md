# memory/maintainer-prompts

Per-session maintainer prompts, archived for provenance. Mirrors `MAINTAINER-PROMPTS/` at the repo root but scoped to the memory subsystem's chronological record. Empty until the first prompt is archived.
